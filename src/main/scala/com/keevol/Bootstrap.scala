package com.keevol


object Bootstrap {
  def main(args: Array[String]): Unit = {
    // val config = new Konfig()
    // val host = config.get("server.host")
    // val port = config.get("server.port")

//    val webServer = new Kate(Array(new RouteRegister {
//      override def apply(router: Router): Unit = router.route("/").handler(ctx => ctx.response().end("fuck it"))
//    }))
//    webServer.start("localhost", 7999)
  }
}